server:
  port: 8080
  mode: debug  # debug, release, test

database:
  host: localhost
  port: 3306
  name: agent_infra
  user: root
  password: ""

redis:
  host: localhost
  port: 6379
  db: 0

log:
  level: info
  format: json
